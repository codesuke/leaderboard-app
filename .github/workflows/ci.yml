name: CI

# TODO (BOOTSTRAP.md step 4): this workflow is a stub. Fill in the checks job
# below for this project's actual stack, then switch the trigger from
# `workflow_dispatch` (manual only) to `push`/`pull_request` so it runs
# automatically. For reference, see ListItUp's ci.yml:
# https://github.com/VirtuNode-dev/ListItUp/blob/main/.github/workflows/ci.yml
#
# on:
#   pull_request:
#     branches: [main]
#   push:
#     branches: [main]

on:
  workflow_dispatch:

jobs:
  checks:
    name: lint, typecheck, test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      # TODO: set up the language/package manager toolchain, e.g.:
      # - uses: actions/setup-node@v6
      #   with:
      #     node-version: 24
      #     cache: npm

      - name: placeholder
        run: |
          echo "CI is not configured yet for this project's stack."
          echo "See BOOTSTRAP.md step 4."
